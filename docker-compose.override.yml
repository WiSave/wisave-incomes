# Dev override: re-exposes WebAPI on host for running portal outside Docker.
# Usage: docker compose up (auto-merges override)
# Production: docker compose -f docker-compose.yml up (skips override)
services:
  wisave-incomes-webapi:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "5300:8080"
